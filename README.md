Sources for the plugin `plugin.gameanalytics_v2`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.gameanalytics_v2" = {
            publisherId = "com.gameanalytics",
        },
    },
}
```
